# TODO: clean out the Docker and lxc images I made earlier

== global setup

sudo apt install schroot debootstrap
sudo mkdir -p /srv/anki

== create schroot

# Vary the path, and the schroot config filename, header, and description.

sudo mkdir /srv/anki/chroot
sudo debootstrap jessie /srv/anki/chroot http://deb.debian.org/debian/

sudo tee /etc/schroot/chroot.d/anki.conf >/dev/null <<EOF
[anki]
description=Anki on jessie
type=directory
directory=/srv/anki/chroot
profile=desktop
preserve-environment=true
users=greg
EOF

== in schroot

apt install -y anki locales fonts-vlgothic mplayer
echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen && locale-gen
ln -nsf /usr/share/zoneinfo/America/Los_Angeles /etc/localtime

== to use

schroot -c anki -- anki
